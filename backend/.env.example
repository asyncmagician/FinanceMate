# Backend Environment Variables
# Copy this file to .env and update with your actual values

# Server Configuration
PORT=3001
NODE_ENV=production

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=financemate
# IMPORTANT: Generate a strong password for production
DB_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
DB_NAME=financemate

# JWT Configuration
# CRITICAL: Generate a secure 256-bit random string for production
# You can use: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING_MIN_32_CHARS
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# Encryption Configuration
# CRITICAL: Must be exactly 32 characters for AES-256-GCM encryption
# Generate with: openssl rand -hex 16
ENCRYPTION_KEY=CHANGE_THIS_TO_32_CHARACTER_KEY

# Email Configuration (Optional - for password reset and notifications)
# Sign up at https://resend.com for free tier (3000 emails/month)
RESEND_API_KEY=re_YOUR_RESEND_API_KEY_HERE
EMAIL_FROM=noreply@your-domain.com
# For development testing only - use a verified email address
TEST_EMAIL=your-test-email@example.com

# Production Security Requirements:
# 1. JWT_SECRET must be at least 256 bits (32+ characters) of random data
# 2. DB_PASSWORD must be strong (16+ chars, mixed case, numbers, symbols)
# 3. ENCRYPTION_KEY must be exactly 32 characters
# 4. Never commit the actual .env file to version control
# 5. Rotate secrets regularly
# 6. Use environment-specific .env files (.env.production, .env.development)
# 7. Keep RESEND_API_KEY secure - it can send emails from your domain